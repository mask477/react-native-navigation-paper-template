import * as React from "react";

import HomeSvg from "../../../assets/icons/home.svg";

const Home = ({ width = 40, height = 40, color = "white" }) => (
  <HomeSvg width={width} height={height} color={color} />
);

export default Home;
